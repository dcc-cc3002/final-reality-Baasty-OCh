package model.party
import model.playable.{Character, MagicCharacter, Playable}

import scala.collection.mutable.Map
//class Party {
  //val allies : Map[String, Playable] = Map()
  //def this(troops: Set[Playable]) = {
    //this()
    //for(m <- troops) {appendPlayable(p)}
  //}
  //def this (p1: Playable,p2:Playable) = {
    //this (Set(p1,p2))
  //}

//}
